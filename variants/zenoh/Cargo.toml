[package]
name = "variant-zenoh"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "variant-zenoh"
path = "src/main.rs"

[dependencies]
anyhow = "1"
variant-base = { path = "../../variant-base" }
zenoh = "1"
clap = { version = "4", features = ["derive"] }

[dev-dependencies]
tempfile = "3"
