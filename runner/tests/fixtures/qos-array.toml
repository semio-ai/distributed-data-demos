run = "qosarr"
runners = ["local"]
default_timeout_secs = 30

[[variant]]
name = "dummy"
binary = "../variant-base/target/release/variant-dummy.exe"

  [variant.common]
  tick_rate_hz = 10
  stabilize_secs = 0
  operate_secs = 1
  silent_secs = 0
  workload = "scalar-flood"
  values_per_tick = 2
  qos = [1, 2]
  log_dir = "./test-logs-qos-array"

  [variant.specific]
